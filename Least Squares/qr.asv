function [q r]=qr_customed(A)
%% parameters:
% input:matrix A
% output:q orthogonal matrix; r right triangular matrix
%% code:
[m n]=size(A);
q=ones(m);
for i=1:n
    x=A(i:end,i);
    w=-sign(A(i,i))*norm()
    H=I-2*v*v'/(v'*v);
    
end
end